# Copilot Instructions

## Project Guidelines
- User prefers using a user-supplied image_tag in the GitHub Actions workflow for deploy-only AKS manifest changes instead of relying on latest.
- User prefers commands to be provided in a single-line format.
- User prefers command lists without bullet prefixes or leading punctuation, and wants future commands presented plain on separate single lines.
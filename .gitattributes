# Enforce LF line endings for all text files in this repo.
# This eliminates "LF will be replaced by CRLF" warnings on Windows.
* text=auto eol=lf

# Explicitly binary — never touch line endings
*.png  binary
*.jpg  binary
*.ico  binary
*.gz   binary
*.zip  binary
*.vsidx binary
